import { ChangeDetectorRef, ElementRef, OnInit, QueryList } from '@angular/core';
import { PanelService } from '../../services/panel/panel.service';
import { Session } from 'openvidu-browser';
import { CaptionModel } from '../../models/caption.model';
import { CaptionService } from '../../services/caption/caption.service';
import { OpenViduService } from '../../services/openvidu/openvidu.service';
import { ParticipantService } from '../../services/participant/participant.service';
import * as i0 from "@angular/core";
/**
 * @internal
 */
export declare class CaptionsComponent implements OnInit {
    private panelService;
    private openviduService;
    private participantService;
    private captionService;
    private cd;
    scrollContainer: QueryList<ElementRef>;
    set captionEventRef(captionEventsRef: QueryList<ElementRef>);
    settingsPanelOpened: boolean;
    captionEvents: CaptionModel[];
    session: Session;
    private deleteFirstTimeout;
    private deleteAllTimeout;
    private DELETE_TIMEOUT;
    private MAX_EVENTS_LIMIT;
    private captionLanguageSubscription;
    private captionLangSelected;
    private screenSizeSub;
    private panelTogglingSubscription;
    constructor(panelService: PanelService, openviduService: OpenViduService, participantService: ParticipantService, captionService: CaptionService, cd: ChangeDetectorRef);
    ngOnInit(): Promise<void>;
    ngOnDestroy(): Promise<void>;
    onSettingsCliked(): void;
    private subscribeToTranscription;
    private updateCaption;
    private deleteFirstEventAfterDelay;
    private deleteAllEventsAfterDelay;
    private subscribeToCaptionLanguage;
    private subscribeToPanelToggling;
    private scrollToBottom;
    static ɵfac: i0.ɵɵFactoryDeclaration<CaptionsComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<CaptionsComponent, "ov-captions", never, {}, {}, never, never, false>;
}
